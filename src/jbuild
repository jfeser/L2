(jbuild_version 1)

(ocamllex (lexer))

(menhir ((modules (parser))))

(executables
 ((names (l2 tests))
  (libraries (core oUnit yojson))
  (preprocess (pps (ppx_jane)))))

(install
 ((section bin)
  (files ((l2.exe as l2)))))
